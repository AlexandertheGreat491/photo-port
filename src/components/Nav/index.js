// imports react
import React from 'react';

// creates the nav component on the page
function Nav() {
    return (
        <header>
        </header>
    );
}

// exports the Nav component
export default Nav;